services:
  focusly:
    build: .
    ports:
      - "3847:3847"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3847
      FOCUSLY_URL: ${FOCUSLY_URL:-http://localhost:3847}
    restart: unless-stopped
